<div class="container mt-5">
  <h2>Edit ride</h2>
  <%= render 'form', url: ride_path(@ride), method: :patch %>
  <div class="mt-3">
    <%= link_to 'Back', rides_path, class: "btn btn-info btn-sm" %></p>
  </div>
</div>
